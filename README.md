# Restart HNS (Host Network Service)

A simple Windows batch script to restart the Host Network Service (HNS), useful for Docker network issues and troubleshooting.

## Usage

Run the script as administrator:

```cmd
HNS.bat
